Python Interface
================

The Python interface is intentionally small. Build runtime objects from
packaged suite collections, YAML files, or YAML-shaped dictionaries. Start with
the examples page: it is written as complete copy-pasteable Python workflows.

.. toctree::
   :maxdepth: 2

   examples
   api
